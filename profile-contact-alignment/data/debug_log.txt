debug log:

1. In RNAlib, `rna_file_msa_read` may return an empty `RNAss` value.
2. G-U base-pair scores may be 0.9 or 0.1. This needs confirmation.
3. `CMakeLists.txt` needs the RNAlib2 library path in `PKG_CONFIG_PATH`; otherwise CMake can fail.
4. MSA processing may still contain bugs.
5. In multithreaded alignment, all Map B state must use local variables to keep alignment results correct. `temseq` should also be local.
